{
  "name": "slack-upload",
  "version": "1.0.6",
  "description": "Easy to upload files to Slack channels through this command line utility.",
  "main": "index.js",
  "bin": { "slack-upload" : "bin/slack-upload.js" },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tisrico",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0",
    "node-slack-upload": "^1.2.1"
  }
}
